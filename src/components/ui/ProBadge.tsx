'use client';

/** ProBadge — small pill shown next to Pro-only feature titles. */
export function ProBadge() {
  return (
    <span className="inline-flex items-center rounded-full bg-amber-500/15 border border-amber-500/25 px-1.5 py-0.5 text-[10px] font-bold text-amber-400 tracking-wide uppercase">
      Pro
    </span>
  );
}
